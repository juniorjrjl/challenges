/*const {  } = require('./challenge_22')

describe('', 
    () => it.each([
        [,], 
        [,], 
        [,],
        [], 
    ])
        ('', 
        (, expected) => expect(()).toBe(expected)))
*/