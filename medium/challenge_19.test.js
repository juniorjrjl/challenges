/*const {  } = require('./challenge_19')

describe('', 
    () => it.each([
        [,], 
        [,], 
        [,],
        [], 
    ])
        ('', 
        (, expected) => expect(()).toBe(expected)))
*/