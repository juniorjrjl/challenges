/*const {  } = require('./challenge_21')

describe('', 
    () => it.each([
        [,], 
        [,], 
        [,],
        [], 
    ])
        ('', 
        (, expected) => expect(()).toBe(expected)))
*/