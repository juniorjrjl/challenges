/*const {  } = require('./challenge_25')

describe('', 
    () => it.each([
        [,], 
        [,], 
        [,],
        [], 
    ])
        ('', 
        (, expected) => expect(()).toBe(expected)))
*/